#include <bits/stdc++.h>
using namespace std;

int main() {
    int arr1[3][2] = {2, 5, 8, 1, 9, 7};
    int arr2[3][2] = {{2, 5}, {8, 1}, {9, 7}};  // both the declarations are same. ( arr1 = arr2 )
    // both the arrays arr1 & arr2 are stored in same way in memory i.e. in contiuous memory locations.
}